using UnityEngine;

public class Item : MonoBehaviour
{
    //public InventoryItem itemData;

    //public void PickUp()
    //{
    //    bool result = PlayerManager.instance.GetComponent<InventoryManager>().AddItem(itemData.item, itemData.amount);
    //    if (result)
    //    {
    //        Destroy(gameObject);
    //    }
    //}
}
